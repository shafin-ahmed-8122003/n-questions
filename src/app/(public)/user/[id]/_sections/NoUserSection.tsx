const NoUserSection = () => {
    return (
        <main className="w-full h-full flex flex-col items-center justify-center">
            <h1>{"There's no user like this! Get out!"}</h1>
        </main>
    );
};

export default NoUserSection;
